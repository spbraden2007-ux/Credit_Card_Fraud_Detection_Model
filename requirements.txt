# Credit Card Fraud Detection - Ensemble ML Pipeline
# ===================================================
# Dependencies for EllipticEnvelope + LightGBM with Optuna optimization

# Core Data Science Stack
numpy>=1.21.0       # Array operations, numerical computing
pandas>=1.3.0       # DataFrame manipulation, CSV I/O

# Machine Learning
scikit-learn>=1.0.0 # EllipticEnvelope (anomaly detection), metrics, cross-validation
lightgbm>=3.3.0     # Gradient boosting classifier (DART mode)
optuna>=3.0.0       # Bayesian hyperparameter optimization (TPE sampler)

# Utilities
torch>=1.10.0       # Top-k selection for pseudo-labeling (torch.topk)
tqdm>=4.62.0        # Progress bars for cross-validation loops

# Python version requirement:
# Python >= 3.8

# Installation:
# pip install -r requirements.txt --break-system-packages

# Alternative (if using conda):
# conda install numpy pandas scikit-learn lightgbm optuna pytorch tqdm -c conda-forge

# Notes:
# - torch is only used for efficient top-k selection (can replace with numpy.argpartition if needed)
# - lightgbm requires OpenMP on some systems: brew install libomp (macOS)
